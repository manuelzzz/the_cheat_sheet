# Agent Instructions

This project uses shared, tool-agnostic rules.

Before making changes, read all relevant files inside:

`.rules/`

The `.rules/` directory is the source of truth for project conventions and architecture.

Do not duplicate rules from `.rules/` in this file.

When instructions conflict, use the following priority:

1. Explicit user instructions.
2. Relevant `.rules/` instructions.
3. Existing project conventions.
